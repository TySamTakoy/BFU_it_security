ЗАДАНИЕ 2:
Написать пару клиент и сервер. Сервер получает строку чисел, считает и посылает назад их сумму. Клиент устанавливает соединение,
посылает на сервер несколько чисел и выводит то, что пришло с сервера на экран. Вся передача информации должна шифроваться SSL/TLS.

serverCreator.py - создает сервер в папке server с самоподписанным сертификатом (cert.pem) и открытым ключом (key.pem)

server.py и client.py - сервер и клиент соответственно

Порядок работы:

1. Удалить папку server и ее содержимое, если необходимо пересоздать сервер, сертификат и ключ.

2. Запустить скрипт serverCreator.py .

3. Запустить скрипт server.py .

4. Ввести в клиенте числа через пробел/запятую.

5. Проверить получение на сервере и ответ клиенту.